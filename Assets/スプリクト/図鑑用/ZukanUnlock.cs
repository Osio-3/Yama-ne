/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ZukanUnlock : MonoBehaviour
{
    public string zukanKey;   // rabbit ‚Æ‚©
    private bool unlocked = false;

    void OnEnable()
    {
        if (unlocked) return;

        unlocked = true;
        ZukanManager.Instance?.Unlock(zukanKey);
    }
}
*/